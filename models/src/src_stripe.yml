version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment  
        columns:
          - name: amount
            tests:
              - dbt_utils.expression_is_true:
                  expression: "> 0"
