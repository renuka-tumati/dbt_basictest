version: 2

models:
  - name: fact_orders
    description: One record per Order
    columns:
      - name: payment_id
        description: Primary key
        tests:
          - not_null
          - unique
          - relationships:
              to: ref('stg_payments')
              field: payment_id
          



